import type { Meta, StoryObj } from '@storybook/react';
import { Video2 } from './Video2';

const meta: Meta<typeof Video2> = {
  title: 'BYD Figma Components/Video2',
  component: Video2,
};

export default meta;
type Story = StoryObj<typeof Video2>;

export const Default: Story = {};
